from itertools import groupby

def compress_string():
    s = input()
    results = []
    for k, g in groupby(s):
        count = len(list(g))
        results.append(f"({count}, {k})")
    print(" ".join(results))

if __name__ == '__main__':
    compress_string()
